package com.example.demo.webAutoTest;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.testng.annotations.AfterTest;
import org.testng.annotations.BeforeTest;
import org.testng.annotations.Parameters;
import org.testng.annotations.Test;



/**
 * @Author: xinyi
 * @Description:
 * @Date: Created In 9:11 2018/8/3
 * @Modified By:
 */
public class TestDemo {
/*    WebDriver driver;

    @BeforeTest
    public void init() {
        String url = "http://www.baidu.com";

        //新建一个浏览器句柄
        driver = new Brower().chrome();
        //打开URL
        driver.get(url);
    }

    @Test

    public void search() throws InterruptedException {
        //输入搜索字符串
        driver.findElement(By.xpath("//*[@id=\"kw\"]")).sendKeys("小可爱");
        //点击[百度一下]按钮
        driver.findElement(By.xpath("//*[@id=\"su\"]")).click();
        Thread.sleep(1000);
        driver.findElement(By.xpath("//*[@id=\"kw\"]")).clear();
        driver.findElement(By.xpath("//*[@id=\"kw\"]")).sendKeys("小胖猪");
        //点击[百度一下]按钮
        driver.findElement(By.xpath("//*[@id=\"su\"]")).click();
        Thread.sleep(1000);
    }

    @AfterTest
    public void teardown() {
        driver.quit();
    }*/

}
